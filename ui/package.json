{
  "name": "book-wunderwaffe-studio",
  "version": "3.0.0",
  "description": "Local-first multi-project book, narration and video ecosystem",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5178",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4178",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "qrcode.vue": "^3.5.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "vite": "^7.0.0"
  }
}
